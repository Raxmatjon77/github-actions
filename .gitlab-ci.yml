image: node:20  # Use Node.js image (includes curl, bash, etc.)

services:
  - docker:dind

stages:
  - build
  - deploy

variables:
  AAG5ERdmz3za_N5YXM9JPp8JTMO6XEqcAv8: "token"
  1955031743: "chat_id"

build-job:
  stage: build
  script:
    - apt-get update && apt-get install -y curl  # Install curl
    - |
      send_message() {
        curl -s -X POST "https://api.telegram.org/bot${AAG5ERdmz3za_N5YXM9JPp8JTMO6XEqcAv8}/sendMessage" \
          -d chat_id="${1955031743}" \
          -d text="$1"
      }
      send_message "🚀 Build started\nBranch: ${CI_COMMIT_BRANCH}\nAuthor: ${CI_COMMIT_AUTHOR}\nMessage: ${CI_COMMIT_MESSAGE}\nTime: $(date '+%Y-%m-%d %H:%M:%S')"
    - docker build -t qazo-namoz-image .
  only:
    - main
  after_script:
    - |
      send_message() {
        curl -s -X POST "https://api.telegram.org/bot${AAG5ERdmz3za_N5YXM9JPp8JTMO6XEqcAv8}/sendMessage" \
          -d chat_id="${1955031743}" \
          -d text="$1"
      }
      if [[ "$CI_JOB_STATUS" == "failed" ]]; then
        send_message "❌ Build failed\nBranch: ${CI_COMMIT_BRANCH}\nAuthor: ${CI_COMMIT_AUTHOR}\nMessage: ${CI_COMMIT_MESSAGE}\nTime: $(date '+%Y-%m-%d %H:%M:%S')"
      fi
