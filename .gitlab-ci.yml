stages:
  - build
  - deploy

build-job:
  stage: build
  script:
    - docker build -t qazo-namoz-image .
  only:
    - main

deploy-job:
  stage: deploy
  script: 
    - docker-compose up -d
  only:
    - main
