config:
  target: 'http://localhost:4001'  # Your NestJS app URL
  phases:
    - duration: 120
      arrivalRate: 50  # 10 new users per second
scenarios:
  - flow:
   
      - post:
          url: "/auth/verify"
          json:
            id: 1
            code: "value2"
